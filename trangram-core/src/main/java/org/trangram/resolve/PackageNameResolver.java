package org.trangram.resolve;

import org.trangram.config.SchemaContext;
import org.trangram.config.TableContext;
import org.trangram.config.TrangramContext;

/**
 * project : org.trangram
 * author :  liyuqi
 * date : 2017-05-05 10:37
 */
public class PackageNameResolver implements NameResolver {
    @Override
    public String resolve(String placeholder, TrangramContext context, SchemaContext schemaContext, TableContext tableContext) {
        return null;
    }
}
